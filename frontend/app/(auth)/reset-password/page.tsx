const page = () => {
  return (
    <section
      className={`bg-brand-secondary text-foreground h-screen flex justify-center items-center`}
    >
      ss
    </section>
  );
};

export default page;
